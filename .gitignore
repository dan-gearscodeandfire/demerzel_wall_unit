# MicroPython boot file with WiFi credentials
tests/boot.py

# Server env files with secrets (HA token, etc) - never commit
server/env
server/env.local
**/env

# Python venv
.venv/

# Python bytecode
**/__pycache__/
*.pyc

# Recordings and audio scratch files (entire scratch dir)
tests/audio/
tests/rec_*.raw
tests/rec_*.wav
tests/.gui_record.log
tests/.tmp_playback.wav

# Session 45 wake-phrase audition samples (large, not used by current validation)
wake_word_samples/

# Firmware binaries (large, downloadable)
firmware/*.bin

# Arduino tooling cache + binaries (downloadable, not committed)
tools/arduino15/
tools/arduino-cli.zip
tools/arduino-cli.exe
tools/LICENSE.txt
tools/arduino-cli.yaml

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*~
